import React from 'react'
import ReactDOM from 'react-dom/client'
import Fullscreen from './src/fullscreen/Fullscreen'
import './src/index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Fullscreen />
  </React.StrictMode>,
)

